import React from 'react';
import LoginForm from '@/components/auth/userAuth/Login';

const LoginPage = () => {
  return (
    <div>
        <LoginForm />
    </div>
  );
};

export default LoginPage;